package controller.dialogs;

import javafx.fxml.FXMLLoader;
import javafx.fxml.Initializable;
import javafx.scene.Node;
import javafx.scene.layout.Pane;
import javafx.fxml.FXML;

import java.io.IOException;
import java.net.URL;
import java.util.ResourceBundle;

public class toolOptionGeneral implements Initializable
{
//    public toolOptionGeneral() throws IOException
//    {

        // attach FXML to this control instance
//        FXMLLoader loader = new FXMLLoader(getClass().getResource("/FXML/controls/toolOptionGeneral.fxml"));
//        loader.setRoot(this);
//        loader.setController(this);
//        loader.load();
//    }

//    @FXML
//    public void initialize() {
//
//        // listen for checkbox changes
////        answer.selectedProperty().addListener((obs, oldVal, newVal) -> {
////            System.out.println("Answer changed to: " + (newVal ? "Yes" : "No"));
////        });
//    }

    @Override
    public void initialize(URL url, ResourceBundle resourceBundle)
    {

    }
}
